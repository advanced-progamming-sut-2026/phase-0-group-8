package ir.hamgit.ahh.PvZ.model.enums;

/** Which top-level menu is currently active. Owned by the menu/controller
 *  teammate; kept here only because every package needs it to compile. */
public enum MenuState {
    REGISTER, LOGIN, MAIN, GAME, SETTINGS, NEWS, PROFILE, COLLECTION, PLANT_SELECT,
    IN_GAME, GREENHOUSE, TRAVEL_LOG, LEADERBOARD, SHOP, MINIGAME
}
